 # if gv in phi.get('sigma', {}):
        #     conditions = phi['sigma'][gv]
        #     if not all(eval(cond, {}, row) for cond in conditions):
        #         continue  # Skip initializing this aggregate field if row doesn't satisfy sigma